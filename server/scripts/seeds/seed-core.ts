// import clientPromise from '../utils/mongo'
import clientPromise from '../../db/mongo';

export default async function seedCore() {
  // const client = await clientPromise
  // const db = client.db('pros4noobs')
  // const techs = db.collection('technologies')

  // const technologies = [
  //   { _id: 'react', label: 'React', category: 'frontend' },
  //   { _id: 'vue', label: 'Vue.js', category: 'frontend' },
  //   { _id: 'node', label: 'Node.js', category: 'backend' },
  //   // ... outros
  // ]

  // await techs.deleteMany({})
  // await techs.insertMany(technologies)

  // console.log('Core seed: technologies inserted.')
}
