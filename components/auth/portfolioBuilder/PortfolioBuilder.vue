<script setup lang="ts">
  // import { ref } from 'vue';
  // import { nanoid } from 'nanoid';
</script>

<template>
  <div>
    portfolio builder component
    <!-- <h1 class="text-2xl font-bold mb-4">Montador de Portfólio</h1>

    <div class="space-y-4">
      <PortfolioBlockEditor
        v-for="(block, index) in blocks"
        :key="block.id"
        v-model:block="blocks[index]"
        @delete="removeBlock(index)"
      />
    </div>

    <div class="mt-6">
      <button @click="addBlock('bio')">+ Bio</button>
      <button @click="addBlock('projects')">+ Projetos</button>
      <button @click="addBlock('stack')">+ Stack</button>
      <button @click="addBlock('customText')">+ Texto livre</button>
    </div>

    <div class="mt-10">
      <h2 class="text-xl font-semibold">Pré-visualização:</h2>
      <PortfolioBlockRenderer :blocks="blocks" />
    </div> -->
  </div>
</template>
